# LLMLab - System Diagrams & Visualizations

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                          LLMLab Architecture                     │
└─────────────────────────────────────────────────────────────────┘

                         ┌──────────────────┐
                         │  Developer Code  │
                         │  (Using LLM API) │
                         └────────┬─────────┘
                                  │
                ┌─────────────────┼─────────────────┐
                │                 │                 │
         ┌──────▼────┐     ┌──────▼────┐    ┌──────▼────┐
         │  OpenAI   │     │ Anthropic │    │  Google   │
         │   (GPT)   │     │ (Claude)  │    │ (Gemini)  │
         └──────┬────┘     └──────┬────┘    └──────┬────┘
                │                 │                 │
                └─────────────────┼─────────────────┘
                                  │
                         ┌────────▼────────┐
                         │  LLMLab SDK     │
                         │  @track_cost    │
                         └────────┬────────┘
                                  │
                ┌─────────────────┼─────────────────┐
                │                 │                 │
         ┌──────▼────┐     ┌──────▼────┐    ┌──────▼────┐
         │   CLI     │     │  Frontend  │    │   Web API │
         │ (Click)   │     │  (React)   │    │ (FastAPI) │
         └──────┬────┘     └──────┬────┘    └──────┬────┘
                │                 │                 │
                └─────────────────┼─────────────────┘
                                  │
                         ┌────────▼────────┐
                         │ Backend Server  │
                         │ (FastAPI)       │
                         ├─────────────────┤
                         │ /api/track      │
                         │ /api/status     │
                         │ /api/budget     │
                         │ /api/optimize   │
                         │ /api/export     │
                         └────────┬────────┘
                                  │
                         ┌────────▼────────┐
                         │   Database      │
                         │ Supabase PG     │
                         │ (or JSON files) │
                         └─────────────────┘
```

## Data Flow - Cost Tracking

```
┌─────────────────────────────────────────────────────────────────┐
│                      Cost Tracking Flow                          │
└─────────────────────────────────────────────────────────────────┘

Step 1: User Code Executes
────────────────────────────────────────────────────────────────
  @track_cost(provider="openai", model="gpt-4")
  def summarize_text(text):
      response = openai.ChatCompletion.create(...)
      return response
  
  summary = summarize_text("...")  # Triggers decorator


Step 2: SDK Extracts Costs
────────────────────────────────────────────────────────────────
  response = {
      "choices": [...],
      "usage": {
          "prompt_tokens": 100,
          "completion_tokens": 50,
          "total_tokens": 150
      }
  }
  
  cost_usd = 0.003  # Calculated from token counts + pricing


Step 3: Backend Recording
────────────────────────────────────────────────────────────────
  POST /api/track
  {
      "project_id": "my-app",
      "provider": "openai",
      "model": "gpt-4",
      "input_tokens": 100,
      "output_tokens": 50,
      "cost_usd": 0.003,
      "feature": "summarization"
  }


Step 4: Status Query
────────────────────────────────────────────────────────────────
  GET /api/status/my-app
  
  Response:
  {
      "total_spend": 0.15,
      "monthly_limit": 100,
      "usage_percent": 0.15,
      "by_provider": {"openai": 0.15},
      "by_model": {"gpt-4": 0.15}
  }


Step 5: CLI Display
────────────────────────────────────────────────────────────────
  $ llmlab status
  
  📊 LLM Cost Status - my-app
  
  Total Spent:         $0.15
  Monthly Limit:       $100.00
  Usage:               0.2%
  Daily Average:       $0.03
  Days Remaining:      30
  Events Tracked:      50
  
  💰 By Provider:
  ┌──────────┬─────────┐
  │ Provider │ Cost    │
  ├──────────┼─────────┤
  │ openai   │ $0.15   │
  └──────────┴─────────┘
```

## Database Schema

```
┌─────────────────────────────────────────────────────────────────┐
│                      Database Structure                          │
└─────────────────────────────────────────────────────────────────┘

PROJECTS
┌─────────────────┐
│ project_id (PK) │──┐
│ name            │  │
│ created_at      │  │
│ updated_at      │  │
└─────────────────┘  │
                     │
      ┌──────────────┼──────────────┬──────────────┐
      │              │              │              │
      │ 1:N          │ 1:N          │ 1:N          │
      │              │              │              │
      ▼              ▼              ▼              ▼
┌─────────────┐ ┌─────────┐ ┌──────────────┐ ┌─────────────┐
│COST_EVENTS  │ │BUDGETS  │ │RECOMMENDATIONS│ │SETTINGS     │
├─────────────┤ ├─────────┤ ├──────────────┤ ├─────────────┤
│event_id (PK)│ │budget_id│ │rec_id (PK)   │ │setting_id   │
│project_id   │ │project_ │ │project_id    │ │project_id   │
│provider     │ │  id     │ │title         │ │key          │
│model        │ │monthly_ │ │description   │ │value        │
│input_tokens │ │  limit  │ │savings       │ │created_at   │
│output_tokens│ │alert_   │ │confidence    │ │updated_at   │
│cost_usd     │ │threshold│ │created_at    │ └─────────────┘
│feature      │ │created_ │ └──────────────┘
│timestamp    │ │  at     │
└─────────────┘ └─────────┘
```

## CLI Command Flow

```
┌──────────────────────────────────────────────────────────────┐
│              LLMLab CLI Command Flow                         │
└──────────────────────────────────────────────────────────────┘

llmlab init
│
├─ Read user input
│  ├─ Project ID
│  └─ API URL
│
├─ Create config dir (~/.llmlab)
├─ Save config.json
│
└─ Print success message
   └─ Show next steps


llmlab status
│
├─ Load config
├─ GET /api/status/{project_id}
│
├─ Parse response
├─ Format with tabulate
│
├─ Display:
│  ├─ Total Spent
│  ├─ Monthly Limit
│  ├─ Usage Percentage
│  ├─ Daily Average
│  ├─ Days Remaining
│  ├─ By Provider (table)
│  └─ By Model (table)
│
└─ Show alerts if needed
   ├─ Budget exceeded
   └─ Approaching limit


llmlab optimize
│
├─ Load config
├─ GET /api/optimize/{project_id}
│
├─ Parse recommendations
│
└─ Display for each:
   ├─ Title
   ├─ Description
   ├─ Savings
   └─ Confidence %
```

## API Endpoint Reference

```
┌──────────────────────────────────────────────────────────────┐
│                 API Endpoint Reference                       │
└──────────────────────────────────────────────────────────────┘

Health & Monitoring
───────────────────
GET /health
└─ Response: {"status": "ok", "timestamp": "2024-02-09T..."}


Cost Tracking
─────────────
POST /api/track
├─ Body: {
│    "project_id": "string",
│    "provider": "openai|anthropic|google|...",
│    "model": "string",
│    "input_tokens": int,
│    "output_tokens": int,
│    "cost_usd": float,
│    "feature": "string" (optional),
│    "timestamp": "string" (optional)
│  }
└─ Response: {"success": true, "event": {...}}


Status & Reporting
──────────────────
GET /api/status/{project_id}
├─ Response: {
│    "project_id": "string",
│    "total_spend": float,
│    "monthly_limit": float,
│    "usage_percent": float,
│    "days_remaining": int,
│    "daily_average": float,
│    "events_count": int,
│    "by_provider": {provider: cost},
│    "by_model": {model: cost}
│  }


Budget Management
─────────────────
POST /api/budget
├─ Body: {
│    "project_id": "string",
│    "monthly_limit_usd": float,
│    "alert_threshold": float (default: 0.8)
│  }
└─ Response: {"success": true, "budget": float}


Optimization
─────────────
GET /api/optimize/{project_id}
├─ Response: {
│    "project_id": "string",
│    "total_cost": float,
│    "recommendations": [
│      {
│        "title": "string",
│        "description": "string",
│        "estimated_savings": float,
│        "confidence": float
│      }
│    ]
│  }


CSV Export
──────────
GET /api/export/{project_id}
├─ Response: {
│    "filename": "string",
│    "content": "CSV formatted data"
│  }
```

## Recommendation Types

```
┌──────────────────────────────────────────────────────────────┐
│          Cost Optimization Recommendation Types              │
└──────────────────────────────────────────────────────────────┘

Type 1: Direct Comparison (90% confidence)
─────────────────────────────────────────
Model Switch: GPT-4 → Claude 3.5 Sonnet

Current:  GPT-4 Turbo
├─ Cost: $0.03 per 1K input tokens
├─ Cost: $0.06 per 1K output tokens
└─ Typical task: 100 requests = $3.00

Recommendation: Claude 3.5 Sonnet
├─ Cost: $0.003 per 1K input tokens
├─ Cost: $0.015 per 1K output tokens
└─ Typical task: 100 requests = $0.30

Savings: 90% ($2.70/month for 100 requests)
Confidence: 90% (similar quality proven)


Type 2: Pattern Matching (70% confidence)
──────────────────────────────────────────
Use Case Pattern Recognition

Current: Claude 3 Opus for all tasks
├─ Cost: High
└─ Usage: 200 calls/week

Pattern Found: Similar tasks in codebase use Haiku
├─ Your "classification" tasks
├─ Your "simple" queries
└─ Match: 40% of your usage

Recommendation: Haiku for simple tasks
├─ Cost: 98% cheaper
├─ Quality: Sufficient for classification
└─ Estimated Savings: 30% overall

Confidence: 70% (requires testing)


Type 3: Low-Risk Wins (99% confidence)
───────────────────────────────────────
Prompt Optimization

Finding: Your prompts average 1500 tokens
Industry Standard: 800 tokens for same quality

Optimization: Remove redundant examples, use shorter phrasing
├─ Reduces token count by 45%
├─ Maintains quality
└─ Zero risk

Estimated Savings: 15% ($150/month on $1000 spend)
Confidence: 99% (proven technique)
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│              Production Deployment Setup                     │
└──────────────────────────────────────────────────────────────┘

Developer Laptop
┌─────────────────┐
│  Local Testing  │
│  ├─ FastAPI    │
│  ├─ React Dev  │
│  └─ CLI        │
└────────┬────────┘
         │ git push
         ▼
    GitHub Repo
    ├─ Main branch
    └─ Triggers CI/CD


Railway (Backend)
┌──────────────────────┐
│ FastAPI Server       │
├──────────────────────┤
│ Env: SUPABASE_URL    │
│      SUPABASE_KEY    │
├──────────────────────┤
│ Health Check: /health│
│ (prevents sleep)     │
└──────────┬───────────┘
           │ API calls
           ▼
    Supabase PostgreSQL
    ├─ projects
    ├─ cost_events
    ├─ budgets
    └─ recommendations


Vercel (Frontend - v1.1)
┌──────────────────────┐
│ React Dashboard      │
├──────────────────────┤
│ Real-time Charts    │
│ Budget Alerts       │
│ Settings UI         │
└──────────┬───────────┘
           │ API calls
           ▼
    Railway Backend


PyPI Registry
┌──────────────────────┐
│ llmlab-cli v1.0.0    │
├──────────────────────┤
│ pip install llmlab   │
│ (available globally) │
└──────────────────────┘
```

---

These diagrams provide visual reference for:
- System architecture
- Data flow
- Database design
- CLI workflows
- API endpoints
- Recommendation logic
- Production deployment

Print or share these diagrams with your team, investors, or in documentation!
